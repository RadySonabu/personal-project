[project]
name = "fastapi-vercel"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "fastapi==0.111.0",
    "uvicorn==0.30.0",
    "sqlalchemy==2.0.30",
    "psycopg[binary]==3.1.18",
]

[tool.uv]

